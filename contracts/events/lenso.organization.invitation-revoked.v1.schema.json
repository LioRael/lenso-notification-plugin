{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lenso.dev/contracts/lenso.organization.invitation-revoked.v1.schema.json",
  "title": "lenso.organization.invitation-revoked.v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["invitationId", "organizationId", "revokedAt"],
  "properties": {
    "invitationId": { "type": "string", "minLength": 1, "maxLength": 240 },
    "organizationId": { "type": "string", "minLength": 1, "maxLength": 240 },
    "revokedAt": { "type": "string", "format": "date-time" }
  }
}
